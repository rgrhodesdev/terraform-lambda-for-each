# terraform-lambda-for-each

aws lambda list-functions

aws lambda update-function-code --function-name <functionname> --zip-file fileb://<code.zip>

aws lambda invoke --function-name <functionname> response.json 
